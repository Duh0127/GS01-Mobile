import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#1B2445",
        flex: 1,
    },
    imgFundo: {
        flex: 1,
        resizeMode: "cover",
        alignItems: "center",
        paddingTop: 50,
    },
});
